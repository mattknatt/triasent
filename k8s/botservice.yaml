apiVersion: apps/v1
kind: Deployment
metadata:
  name: botservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: botservice
  template:
    metadata:
      labels:
        app: botservice
    spec:
      containers:
        - name: botservice
          image: botservice:0.0.1-SNAPSHOT
          imagePullPolicy: Never        # locally built image
          ports:
            - containerPort: 8080
          env:
            # Relaxed-binding overrides for the ${...:default} placeholders in
            # application.properties (RABBITMQ_HOST -> rabbitmq.host, etc.).
            - name: RABBITMQ_HOST
              value: rabbitmq
            - name: SPRING_RABBITMQ_PASSWORD  # rabbitmq 'app' password, from the chart's Secret
              valueFrom:
                secretKeyRef:
                  name: rabbitmq
                  key: rabbitmq-password
            - name: AUTHSERVICE_HOST
              value: authservice          # token endpoint http://authservice:9000/oauth2/token
            - name: MESSAGESERVICE_HOST
              value: messageservice       # reply target http://messageservice:8081/messages
            - name: SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_BOT_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth-clients
                  key: bot-client-secret
            - name: APP_BOT_USER_ID           # shared with authservice/messageservice
              value: "00000000-0000-0000-0000-000000000b07"
          envFrom:
            - secretRef:
                name: llm-credentials     # LLM_API_URL / LLM_API_KEY / LLM_API_MODEL
---
apiVersion: v1
kind: Service
metadata:
  name: botservice
spec:
  selector:
    app: botservice
  ports:
    - port: 8080
      targetPort: 8080
